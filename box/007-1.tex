\begin{mitframe}{007-1} % 20160706MAC

    \mdfsubtitle{Virial (Box 7-1)}
    
\begin{listone}
    
    \item Form
    
    \begin{listtwo}
    
    	\item $\p = \dens\boltz\Temp + \boltz\Temp\sum\limits_{n=2}^\infty\virialcoeff{n}(\Temp)\dens^{n} = \dens\boltz\Temp(1+\virialcoeff{2}\dens+...)$     %'P' is thermodynamic pressure
    % 20160703MAC: 'p' here should be capital 'P' <resolved>
    
    	\item $\virialcoeff{2}(\Temp) = -2\pie\av\int\limits_{0}^{\infty}\Big[\exp[-\boltznorm\pot(\pos)]-1\Big]\pos^2\mathrm{d}\pos$ 
	
    % 20160706MAC: the unit for N_A or '\av' or Avogadro's number is "molecules/mol"
    
    % 20160706MAC: unit for B_n or nth virial coefficient should raised to the "n-1" instead of just "n" <resolved>
    
    	\begin{listthree}
    
    		\item Quantifies the two-body interaction
            
            \begin{listfour}
    
    			\item McMillan-Mayer TM 471

			\end{listfour}

    		\item Hard sphere: $\virialcoeff{2} = \dfrac{2\pie\molecrad^3}{3}$
    
    % 20160706MAC: the unit of sigma or '\molecrad' should just be [m] instead of [A] <resolved>
    
    		\item Square well: $\virialcoeff{2} = \dfrac{2\pie\molecrad^3}{3}\Big[1+(1-exp(\boltznorm\welldepth))(\gasconst^3-1)\Big] $

	% 20160706MAC: sigma or '\molecrad' should be raised to 3. '\molecrad^3' instead of just '\molecrad'
	
    % 20160706MAC: epsilon is not '\dielec'. it is an energy parameter defined as "well depth" or "energy well depth" with units in [J] <resolved>

    		\item Lennard-Jones:
            
            \begin{listfour}
    
    			\item $\virialcoeff{2}(\Temp) = -2\pie\av\int\limits_{0}^{\infty}\bigg[\exp\bigg[-\boltznorm\bigg(4\welldepth\Big[\Big(\dfrac{\molecrad}{\pos}\Big)^{12}-\Big(\dfrac{\molecrad}{\pos}\Big)^6\Big]\bigg)\bigg]-1\bigg]\pos^2\mathrm{d}\pos$
    
    % 20160706MAC: epsilon is not '\dielec'. it is an energy parameter defined as "well depth" or "energy well depth" with units in [J] <resolved>
    
    		\end{listfour}
    
    	\end{listthree}
    
    \end{listtwo}
    
\end{listone}
    
\end{mitframe}